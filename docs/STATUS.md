# Implementation status

This repository is a **development alpha**. The complete agreed performance-platform plan is not finished. The implemented paths below are real code rather than simulated UI, but the platform has not passed the 32-player performance acceptance run or physical iPad/audio-interface validation.

## Implemented

- Score editor overhaul (2026-09-09): continuous ensemble view with a collapsible part list, configurable multi-staff parts, entry toolbar/shortcuts, selection/marquee/drag/delete and local undo, shared meter/key changes, counted repeats/first endings and D.C./D.S. navigation. Performance defaults to all current-player assignments, with an opt-in Show all view that orders assigned parts first. Written dynamics and visual raw MIDI point/note/curve lanes feed typed graph MIDI events; the scheduler prepares repeat traversal and uses engine time. Legacy independent loops remain until explicit conversion. The new native notation metadata retains rational note timing and spelling; MusicXML supports a broader multi-staff subset with loss reporting. See [SCORE_EDITOR.md](SCORE_EDITOR.md). Verification of the isolated score-editor commit: all 153 Rust tests, including typed-MIDI allocation/overflow/subgraph tests, all 20 frontend unit tests, eight affected Chromium cases across focused runs, and the production build pass. Browser coverage includes keyboard edits/undo, Ctrl/Cmd selection, marquee deletion, multi-staff persistence, player filtering/default reset, MIDI curve drawing and graph telemetry, repeat stop/restart, MusicXML round trips, count-in and the existing workspace/monitor flow. Physical MIDI/audio timing, iPad operation, external notation-app interchange and full ensemble-load acceptance remain manual/unverified.

- Pitch tracker (2026-09-08): new Analysis node infers the incoming 1–8-channel audio width, averages to mono and estimates 1–4 ranked pitches with a prepared windowed FFT. Settings select slots, 2048/4096/8192-sample FFT size and detection threshold. Left-to-right readouts show MIDI numbers/note names with matching individual outputs; absent detections use −1. Reducing slots removes disabled connections in one undoable edit; server validation rejects invalid configurations and disabled outputs. All 142 Rust tests, 13 frontend unit tests, frontend/release builds and five affected Chromium cases passed. Synthetic DSP tests cover four ranked tones at 44.1/48/96 kHz, harmonic grouping, silence, low-level rejection, opposite-phase cancellation, inferred eight-channel input and actual graph outputs. Chromium verifies a four-tone mix, strongest-first readouts, slot routing, settings, disabled-port rejection and silence; matching-connection and subgraph regressions also pass. The screenshot was inspected. Real-instrument polyphonic accuracy, octave/missing-fundamental ambiguity, physical-device latency and multi-tracker load/endurance remain unverified.

- Toggle event output (2026-09-08): the checkbox now emits one binary event per state change, with separate latched visual state. Checking emits 1, clearing emits 0, and idle frames emit nothing; restoring a saved unconnected checkbox does not trigger downstream nodes. Event validity survives direct connections, merged inputs, Trigger/visualizer passthrough and named routing, preserving explicit zero commands without idle clearing. Trigger flashes briefly and counters count checking transitions. All 139 Rust tests, 13 frontend unit tests, frontend/release builds and seven affected Chromium cases passed. New software regressions cover sample-level emission, unchanged commands, checkbox mirrors, held parameter values, named-route delay, Trigger reset and counter counts. Physical iOS input and hardware timing remain manual/unverified.

- Expanded piano, integer roots, sample dragging and WebRTC discovery (2026-09-08): Piano has a saved 1–8 octave span dropdown, wider keys and received-note highlighting throughout the selected MIDI range, capped at 127. Display changes preserve externally held notes. Root MIDI note editing uses integer steps, with client/server fractional-value rejection. Sample names in the project sidebar support desktop/touch dragging onto sample-capable nodes or onto empty space to create a polyphonic sampler with sample channels/root defaults. WebRTC now uses LAN ICE-lite and numeric server candidates with mDNS queries disabled, avoiding repeated multicast-send errors. All 138 Rust tests, 13 frontend unit tests, frontend/release builds and six affected Chromium cases passed. The expanded DSP regression covers held notes across span changes and the MIDI upper bound; browser tests cover span validation, existing sample assignment, touch creation, integer root editing, and real monitor audio with deliberately unresolvable browser `.local` candidates. Existing browser input/cancellation coverage also passes. The piano screenshot was inspected. Physical iOS/MIDI devices, the reported host's actual network routes, and hardware timing remain manual/unverified; no server restart was performed automatically.

- Input connection moves and Toggle coercion (2026-09-08): dragging an input moves all incoming edges as a bundle; dropping away from inputs deletes them, while output drags create new edges. Escape cancels, Undo restores one operation, and invalid input destinations preserve the original graph after server rejection. Mouse and Chromium touch paths are covered. Toggle accepts text (including an empty string) and positive numbers as on, and zero/negative numbers as off; its output and saved manual literal remain binary numeric. All 138 Rust tests, 13 frontend unit tests, frontend/release builds and nine affected Chromium cases passed across focused runs, including existing touch gestures/library placement and graph controls. Physical iOS interaction and hardware timing remain manual/unverified.

- Bang → Value → Toggle correction (2026-09-08): triggered Value now holds its last emitted output instead of returning to zero when the bang ends. Explicit emission flags preserve zero and repeated identical commands through control merges, transparent control nodes and named routes, so Toggle reliably accepts on/off commands after manual overrides. Compact Trigger/Value/Toggle connectors sit on their borders; Value places Trigger above Set value. All 137 Rust workspace tests, 13 frontend unit tests, production frontend/release builds and six affected Chromium cases pass. A focused DSP extension also covers named routing and visualizer passthrough; browser coverage checks repeated on/off commands, manual overrides, connector geometry and input ordering. The screenshot was inspected. Physical iOS input and hardware timing remain manual/unverified.

- Sample roots and graph routing (2026-09-08): sample metadata supports optional MIDI root pitches with server-side assignment defaults and preserved manual/connected sampler roots. Control inputs accept multiple sources with sample-order changes and upper-node priority for simultaneous changes; modals identify all sources and the winner. Compact Value nodes show the stored number and labeled Set value/Trigger inputs. Six typed Send/Receive nodes support modal or text-driven target names, project-local fanout, audio summing, control arbitration and spectral frame routing with one-sample virtual hops. Two-node Connect matching ports works through the context menu or C, respecting selection order and left-to-right box selection, with atomic undo and server validation. All 136 Rust tests, 13 frontend unit tests, frontend/release builds and 11 affected Chromium/API cases passed; five new-feature cases passed again after the final selection/label changes. Value's screenshot was inspected. These checks use software rendering and synthetic/browser inputs. Physical iOS/MIDI/audio testing, named-route load/endurance and virtual-path latency alignment remain unverified; upstream latency through named routes is not automatically aligned.

- Compact Trigger and Toggle (2026-09-08): Trigger is now a radio-style momentary button with numeric pass-through and a one-sample manual 1 override, including when connected. A runtime sequence plus a short visual flash makes pulses visible between telemetry updates without extending their DSP duration. Toggle is a latched checkbox with saved manual 0/1 state; changed input sets live state, and manual clicks override until input changes again. Neither telemetry nor input-driven state enters project history. Compact nodes retain connection handles, keyboard controls, context menus and modal access. Server validation covers authority, CSRF, engine state, revisions and binary toggle literals. All 128 Rust tests, 11 frontend unit tests, frontend/release builds, and three affected Chromium cases pass across focused runs. Browser checks cover positive/negative pass-through, manual triggers driving an actual counter, unchanged trigger revisions, toggle persistence and live input overrides, keyboard behavior and rejected requests. Screenshots were inspected. Physical iOS touch and hardware timing remain manual/unverified.

Loop/archive verification (2026-09-08): all 126 Rust tests, 11 frontend unit tests, the production frontend and release server builds, and the three affected Chromium integration cases pass. These are software checks, not physical-device or endurance acceptance.

- Looper: eight independent 1–8-channel tracks, immediate freeform capture/play, graph-clock next-bar quantization with denominator-beat lengths, immediate stop/clear, bounded preallocated capture memory, compatible-edit retention, and disk persistence in project/node-owned float WAVs. Modal Clear buttons and a numbered Clear input delete saved tracks in writer order. Preparation restores loops; disable finalizes partial recordings. Rust regressions cover meter/tempo timing, all tracks, replacement, channel preservation, limits, restoration, deletion ordering and disk errors. Chromium verifies actual loop audio, 3/8 lengths, restart restoration, modal/input clearing and server rejection of invalid requests. Timing checks use software clocks; physical-device timing and sustained disk-load behavior remain unverified.

- Record: multichannel performance archival with inferred input width, pulse-driven Start/Stop, node-name prefixes, unique timestamped WAVs in private project directories under server-root `recordings/`, and ownership/completion sidecars. Uses the current engine sample rate and 32-bit float PCM; there is no separate project bit-depth setting. A fixed render buffer and bounded file-worker queue separate capture from disk I/O; overflow stops recording with an explicit error. Takes split before WAV size limits. Stop/disable/retirement finalize archives; the stopped show does not stop graph capture. Rust tests cover sample boundaries, overflow, width inference, compatible edits, PCM/format preservation, project metadata, private filesystem permissions, separate takes, segment continuity and storage errors. Chromium verifies modal naming, eight-channel capture despite a one-channel node setting, actual WAV headers, independent takes, live-edit continuity, private static-path behavior and disable finalization. The download/list UI and its project-membership authorization are still pending; no archive endpoint is exposed. Physical recording, long-session endurance and abrupt-shutdown recovery remain unverified.

- Polyphonic instruments: the sine synth now accepts the common five MIDI controls alongside direct score notes. A new 64-voice FM synth has independent selectable carrier/modulator waveforms, driven A4-reference frequencies, FM depth and release. New synth nodes default to mono and allow 1–8 identical output channels. Tests cover repeated-pitch releases, MIDI/score ownership isolation, gate fallback, fixed voice capacity, phase continuity, FM behavior, driven frequencies, waveform validation and multichannel audio. The frontend build, 110 Rust tests, 11 frontend unit tests and three affected Chromium cases pass, including piano-driven sine/FM audio with a stopped show, waveform menus, connected frequency sources, mono insertion, four-channel output and score routing. Physical MIDI/audio hardware and alias-free high-depth FM are not claimed.

- Phone layout: a breakpoint at 600 px width, or 500 px height with coarse pointing, reduces header/sidebar/transport spacing and initially fits graph nodes at up to half scale. Rotation refits the graph; dialogs and monitor panels use compact spacing. Frontend build, 11 unit tests, phone portrait/landscape checks and existing Chromium touch gesture/library cases pass. Physical iPhone Safari remains manual/unverified.

- Certificates are stored in the visible, Git-ignored `certs/` directory. Launcher/server startup migrates an existing `.local/ssl/` directory when `certs/` does not exist, preserving certificates and private-key permissions. Isolated TLS/launcher tests and a Chromium development-engine smoke test pass; browser fixtures explicitly force isolated HTTP despite local certificates.

- TLS compatibility: the release server explicitly enables both TLS 1.2 and TLS 1.3. Certificate-verified integration tests force each protocol; both also returned HTTP 200 from the running local server after restart. Physical iPad behavior remains unverified.

- Local HTTPS: initial setup offers a local CA/server certificate; `--setup-ssl` configures it later, `--remove-ssl` removes managed certificates and selects HTTP on restart, and `--no-ssl` overrides both start and update/start launches. Managed HTTPS uses port 443 with HTTP 80 redirects, with explicit port overrides supported. Bash 3.2 syntax/help, isolated launcher tests, 42 server tests, and real TLS integration pass, including CA-verified hostname/IP requests, Secure cookies, redirect path/query preservation, saved settings, HTTP override/removal, and Chromium secure-context/media API availability. Tests use temporary certificates and high ports; no OS trust, services or privileged-port settings were modified. Actual iPad trust, physical microphone/monitor operation and deployment port privileges remain manual/unverified.

Latest automated validation: 104 Rust tests, 11 frontend unit tests, frontend production build, and all 24 standard Chromium cases pass across the full run and focused reruns. Launcher syntax/help and four launcher tests pass; the optional release-binary smoke test remains skipped; startup services are never changed by these tests. Physical hardware and iPad checks remain manual.

- Development engine separated from show transport: enabling the engine loads the project graph, starts graph clocks/audio/MIDI/piano and permits browser monitoring and score edits with show beat zero. Show Pause/Stop/Deactivate releases scheduled score notes while manual graph notes and processing continue. Live modal text drafts survive telemetry updates until saved. Graph and show ownership are separately reported and checked; incompatible project takeover is rejected. Engine disablement unloads the graph after show deactivation. Rust and real Chromium/WebRTC tests cover graph-clock continuity, paused-timeline audio, score editing, manual note survival and monitoring cleanup. Hardware timing remains manual/unverified.

- Sample library: project-only sidebar accordion, full browser of owned/global samples, searchable metadata modal (name, category, tags, description, BPM and key), short previews, multichannel waveform with full playback/scrubbing, and project-sample selection in sampler modals. Import through FFmpeg produces current-rate 32-bit float WAV with channels preserved, within 64 MB/30-second/1–8-channel limits. Project members can access samples uploaded by another author; existing project copies remain accessible after global sharing is removed. Only the sample owner can change Global; origin-project editors can edit metadata. Chromium/API tests exercise FLAC (including six channels), MP3, AIFF, M4A, WAV, conversion bit depth, sharing/access rules, malformed imports, preview/scrubbing, searching and project insertion. These checks do not establish support for every codec/build variant or physical-device preview timing.

- Piano key feedback: local presses immediately invert key colors before a server response; received held notes use the same inversion with cyan/amber outlines. Frontend build and the piano browser regression pass, including delayed-request local feedback, received white/black keys and release.

- Piano test node: compact one-octave keys with matching pitch/velocity/gate/note-on/note-off inputs and outputs, modal octave selection (default C4–B4), engine-driven received-key highlighting and out-of-range note forwarding. Mouse, keyboard and simultaneous touch presses emit velocity 100; cancellation/blur/view removal releases local presses. Enable the audio engine to test connected outputs. Transient note requests validate editor access, loaded graph, node kind and MIDI ranges without adding revisions/undo. The frontend build, 103 Rust tests, 11 frontend unit tests and five affected Chromium integration cases pass, including forwarding, held-note preservation across octave changes and independent polyphonic releases. Physical iPad/MIDI hardware timing remains unverified; abrupt network loss may require engine disablement to clear held notes.

- Library insertion compatibility: node, edge, subgraph and score IDs use a UUID fallback backed by `getRandomValues` when `randomUUID` is unavailable (such as LAN HTTP). Drop parsing errors no longer hide node-creation errors. Collapsed subgraph entries no longer consume hidden layout space and cover node buttons. Frontend build and 11 unit tests pass; desktop click/drop and touch insertion pass with `randomUUID` deliberately disabled, including saved-library entries and malformed-drop handling. Subgraph/workspace browser regressions also pass. Physical iPad Safari remains manual/unverified.

Previous full regression checks: 102 Rust tests, 9 frontend unit tests, frontend build, and all 20 standard Chromium integration cases passed across the suite and focused reruns. Immediate-playback regressions explicitly select Count in: Off; dedicated count-in tests cover its default and enabled behavior. These software tests do not verify physical MIDI, iPad Safari or hardware-clock performance timing.

- Part MIDI sources emit only their assigned score part; physical MIDI input supports named keyboard ports and channel filtering. Both expose numeric pitch, velocity, gate, note-on trigger and pitch-specific note-off trigger. Chords serialize one event every two engine samples, preserving separate releases. MIDI output accepts the same controls; legacy number/value aliases and CC mode remain available. Node settings and live source values are in the modal, with server-side assignment/IO validation.
- Polyphonic sampler: WAV upload, 64 prepared voices, root note, velocity scaling, looping and release, with matching five note inputs. The existing Sample player remains available. MIDI to OSC / OSC to MIDI nodes map those controls to/from exact-address OSC messages containing integer pitch and velocity (zero means off), using the configured system OSC sender/receiver. Native callbacks use bounded queues; sending/encoding occurs outside DSP. Rust tests cover part isolation, reassignment, overlapping/repeated notes, release, synthetic MIDI bytes and actual localhost OSC. Chromium verifies modal settings and the complete part → OSC → sampler → WebRTC audio path. Physical MIDI keyboards/ports, iPad Safari, network timing and overload/endurance behavior remain manual/unverified.

- Count-in transport: compact Off/one-bar/1–32 beat selector, defaulting to one time-signature bar. Engine-sample timing holds the score at zero, clicks to connected master/dedicated browser monitors, and starts performance at the final beat boundary. Pause/Stop cancel; resume skips the count; repeated Play does not restart it. Meter/sample-rate/tempo-change and sequencer-boundary Rust tests pass, as do frontend unit/build checks and Chromium WebRTC tests for both monitor feeds, countdown, cancellation, resume, cold activation with Off, and invalid API values. Physical iPad/audio-device timing and listening latency remain manual/unverified.

- Touch library dragging now supports direct node/subgraph placement, tap-to-insert, vertical list scrolling, and hold-to-drag. Cancelled/outside drops do not insert; compatibility clicks do not duplicate nodes. A non-passive touch guard contains page and scroll-panel edge swipes. Frontend unit/build, desktop library, and Chromium touch/fullscreen regression checks pass; physical iPad Safari fullscreen behavior remains manual/unverified.

- Graph touch gestures: one-finger tap selection and marquee selection, two-finger pan, and midpoint-anchored pinch zoom. The app viewport prevents page scrolling while panels retain their own scrolling. Frontend unit/build and Chromium touch/desktop graph integration checks pass; physical iPad/iOS Safari validation remains manual and unverified.

- Monitor workspace with a left statistics/browser-audio sidebar and two meter rows grouped by physical device. Every discovered input channel has a bar, independent of graph nodes; captured inputs are metered before graph routing. Output bars show only physical channels routed by the active graph, after summing all graph contributions. Green/yellow/red bars include clipping and unavailable states. Server-process CPU and resident memory are sampled off the audio thread. Browser monitoring stays connected across tabs and stage navigation. The saved revision/status appears beneath the project name; the redundant upper-right Ensemble button is removed.
- Discovered native audio inputs/outputs default to enabled. Persisted choices survive rediscovery and disconnection; checkbox interactions save immediately without committing other settings drafts. Changes require an inactive show. Physical device startup/capture/playback remain manual/unverified.

- Saved graph revision history is separate from the live edit counter. Edits update the recoverable working copy and engine immediately; a server timer saves one history snapshot per dirty minute, and Cmd/Ctrl+S or Save revision saves immediately after queued live edits. Clean saves are idempotent, old history is retained, and the header shows saved revision/dirty state. Pending deadlines survive server restarts.

- Native input/output channel routing tables with per-signal physical channel selection or None, repeated input sources, summed output destinations, 1:1/disconnect presets, and a stereo mixdown preset with gain adjustment. Native device frames support physical channels 1–64 while graph signals remain 1–8 channels. Device discovery reports the selected-rate channel count; legacy sequential/offset routing is preserved. Routes persist through the working copy, saved revisions and undo. Physical interface verification and load/endurance testing of the larger native buffers remain pending.

Native input startup now follows audio-engine startup and shutdown; the separate capture button/API command is removed. Selected-input startup failures are surfaced before accepting engine activation, and no selected inputs is valid. The workspace Rust suite plus the empty/unchecked-input regression, frontend build/unit tests, and Chromium integration suite were run for this change. Physical input startup and capture remain manual checks.

- Subgraph library beneath the node library, with private entries, permanently public entries, immutable version selection, owner-only new-version saves, and independent forks. Inserted graphs embed their chosen snapshot and bundled WAV samples with fresh project IDs. Library updates never rewrite existing projects. Earlier private drafts remain private when a later version is published.
- Drag/drop from both libraries controls initial placement. Box selection and Control/Command-click select multiple nodes; group movement saves all positions together, and deletion handles selected subtrees. Right/middle dragging pans.

- Nested subgraphs with breadcrumb navigation and named audio/control/spectral boundary nodes. Parent ports follow boundary labels, widths and FFT formats. Compilation validates and flattens the hierarchy; nesting has no separate depth limit, within the existing 256-node/2,048-edge project budget. Duplicate in the context menu or `D` copies stored settings and complete internal subgraphs using new IDs; external cables are not copied. Subtree deletion and undo work during playback.
- Global clock `tempo` control input changes engine BPM at sample time without resetting phase. One tempo driver is allowed per project; connected transport tempo is read-only. Disconnect retains the current engine tempo for this activation.
- Native input device listing, system enablement and per-node routing, plus disabled/unavailable/capture-off feedback. Browser nodes and monitor setup list browser-exposed microphones, retain device selections locally, request permission explicitly, and report denied/blocked/unavailable access. Native multidevice and real microphone permission/capture behavior still require physical-device testing.

- Live graph editing in all modes, node Edit/Delete context menus, selection deletion with Backspace/Delete, and Ctrl/Cmd+Z graph undo. Space and the Play button activate an inactive show and play, then toggle pause/resume. Text entry/dialog controls retain normal keyboard behavior.
- Sample-rate `audio_to_control` with scale/offset for FM; connected oscillator Frequency follows each sample while manual edits stay smoothed. No through-zero FM or oversampling.
- Spectral magnitude/phase multiply/add and separate 33-point drawn magnitude/phase curves, editable in the modal during playback. Curves are persisted configuration; negative bins mirror positive bins, with DC/Nyquist phase retained. Crossfades, physical timing, and heavy live-edit/spectral load remain unverified.

- `init.sh --uas` pulls the tracked upstream with fast-forward-only Git, rebuilds via `--update`, then launches via `--start`, supporting host/port overrides and saved settings. Pull/build failures prevent startup; automatic merge/rebase/stash and service changes are excluded. Bash 3.2 syntax/help and isolated launcher tests passed, including a real local-remote pull into the updated launcher, mocked compilation/start ordering, failure propagation, conflicting edits/divergence, saved settings, and startup leave-unchanged. No production pull or startup-service changes were performed for verification.

- `init.sh --update` rebuilds the frontend and builds the release server with locked dependencies, reusing up-to-date Cargo artifacts, without interactive setup or startup-service changes. It requires installed build tools; source updates and server restarts remain separate.

- Three transparent visualizer nodes: numeric/string control, multichannel audio spectrogram, and spectral spectrogram plus complete positive-bin magnitude/phase graphs. Analysis/history advances once per DSP block; browser presentation is 20 Hz. History is bounded to 128 columns × 32 bands per channel; graphs allow at most 16 visualizers. No visualization history enters persistence/undo. Visualizer resource use is not covered by the prior load baselines.
- A typed numeric/UTF-8 control-value contract for visualizer chains, with fixed-capacity 256-byte runtime strings, modal-only disconnected input literals, and server rejection of text into numeric-only processors. General string operators and external text-event input remain pending.

- Header gear menu for project settings, system settings, and an autoscrolling project engine console with a standalone tab. Server history is bounded and in-memory; it is not a complete process stdout/stderr archive.
- WAV-master-preserving playback caches at the global sample rate, prepared on upload, current-project settings changes, and lazy loading of other projects. Conversion/loading/setup share an indeterminate progress modal. WebRTC rate adapters preserve phase across packets at non-48-kHz engine rates.
- Manual output latency entries and relative interface delay correction, plus an engine-sample metronome with a browser visual reference and a 60-second stop. Hardware loopback calibration, browser/network latency compensation, and drift correction remain unverified/incomplete.
- Hover/focus waveform snapshots on audio connections: 256 actual source samples per channel, bounded on-demand capture, no project persistence. This is a rough visual preview, not a calibrated oscilloscope.
- Oscillator modal selects sine, triangle, sawtooth, square, or noise; sine remains the default. Sawtooth/square edge correction reduces aliasing. A native output queue bug that inserted periodic silence when driver callbacks exceeded the DSP queue target has been reproduced in simulation and corrected; physical listening confirmation remains pending.

- Server binding defaults to HTTP `0.0.0.0:80` or HTTPS `0.0.0.0:443` (all IPv4 interfaces), including newly generated startup scripts; `PR0_BIND` can override it. `--start --host HOST --port PORT` supplies component overrides via `PR0_HOST`/`PR0_PORT`, including with existing saved launch scripts. Either flag can be used alone; TLS/data settings are retained.
- Vite/Vue/TypeScript application, locally bundled fonts, responsive graph workspace, custom stage-instrument node style, compact math blocks, typed animated cables, and touch/keyboard interactions.
- Native dialog parameter editor, immediate unconnected edits, connected read-only engine values, source navigation, five-second traces, stale status, and undo. Channel width and WAV upload controls are available in relevant node modals.
- Shared engine ownership status broadcasts on activation/deactivation and accompanies reconnect snapshots. Browsers discard older status snapshots, clear inactive telemetry, and disconnect browser audio when a remote conductor deactivates the show. Status reports accepted orchestration commands, not physical device completion.
- Reconnecting WebSocket clients receive a current project snapshot, including edits made while offline; queue overflow also reloads the snapshot. Recovery preserves the stage view and part selection. Retired socket callbacks are ignored.
- Rust workspace, SQLite persistence and revision snapshots, local accounts, Argon2 passwords, invitation-only registration after bootstrap, expiring sessions, project membership, role checks, and single-use invitations.
- Performer stage view with read-only notation/piano roll, part selection, shared bar/beat and tempo, role-aware launch/stop cues, fullscreen access, and a timing-loss warning that holds the score. Browser monitoring persists across workspace/stage navigation and disconnects on show deactivation. Physical device timing is not validated.
- Project settings dialog edits the name, performance mode, and initial tempo during preparation. Remote activation disables edits; newer project revisions require reopening the dialog. Saved settings refresh the project picker.
- Project creation with structured/conducted/freeform modes; shared transport and beat-preserving next-beat tempo updates; mode-aware part startup, next-beat part launch/stop, and per-part playback telemetry/UI.
- Part preparation controls persist names, loop lengths, and default notation/piano-roll display. Switching parts restores each default; performance mode uses it without showing editing controls. Notes outside shortened loops remain stored and produce a playback-boundary warning.
- Score-driven polyphonic synth, scrolling multi-staff notation and piano roll, configurable clefs/keys/meter, part/performer routing, native JSON persistence and multi-part MusicXML interchange with loss reporting.
- Internal synth note ownership isolates overlapping notes and parts sharing an instrument. Part stop/relaunch releases only held notes, and transport Stop resets scheduling at beat zero. Pause/resume restores held notes to their original release times; stopping a paused part clears suspended notes. Internal voice phase/envelopes restart on resume. External MIDI/OSC merges shared pitches until their last release; OSC panic sends note-offs.
- Validated acyclic audio/control/spectral graphs, 1–8 channels, one driver per inlet, finite numeric ranges, prepared buffers, and bounded queues.
- Math/conversion/logic nodes; global/local metronomes, phase-preserving clock ratios, counters, gate, random, LFO, trigger, change/select, sample-and-hold, ramp, and a sample-counted ADSR control envelope with retriggering; a 1–16-step control sequencer with value, index, and pulse outputs.
- Native/browser input nodes, output, channel mapping, mono splitting and merging for 1–8-channel bundles, gain/mixer/meter, oscillator/noise/polyphonic synth, pan/crossfade/audio gate, delay/reverb/envelope follower, compressor/expander/limiter/noise gate.
- Third-/fifth-order Butterworth low/high-pass filters, band-pass/notch, three-/five-band parametric EQ, and a 16-band carrier/modulator vocoder.
- Streaming FFT/real FFT, inverse transforms, magnitude/phase conversion, and spectral gain; preallocated framing and overlap-add. Parallel feed-forward audio paths receive compensation for FFT branch latency.
- FFmpeg audio import to WAV (up to 30 seconds, 1–8 channels), sample playback, and sample phase-vocoder preparation with independent time/pitch controls. Phase-vocoder settings are structural and prepared off-thread before playback.
- Persistent System audio settings select 44.1/48/88.2/96 kHz, 32–1024-frame DSP scheduling blocks, and checked CPAL output interfaces. Audio output node modals select an enabled interface. Bounded callback queues remain f32; the driver chooses its callback buffer. Engine enablement moves to the footer and show activation waits for selected-output startup. Native inputs are selected in System settings, routed individually in node modals, and captured automatically when the audio engine starts. Physical devices and independent-clock drift are not validated.
- Server audio admission reserves one slot per project/user through negotiation and peer cleanup, capped at 32 total sessions. Duplicate offers return HTTP 409 without replacing a live peer. Cancellation marks pending offers; final registration rejects cancelled offers or inactive shows. Cleanup releases failed negotiations and cannot remove a newer session’s reservation.
- Browser audio setup isolates each connection attempt, stops late microphone tracks after cancellation, cancels ICE waits, and ignores callbacks/statistics from retired peers. In-flight offers finish before remote cleanup and replacement; an unresponsive offer can still delay reconnect. Playback-resume failures are handled in the UI.
- WebRTC/Opus master stereo monitor, selectable dedicated monitor outputs, and browser microphone uplinks. A real local Chromium connection is included in the browser test. Diagnostics expose jitter, packet loss, round-trip time, and average jitter-buffer duration.
- MIDI output discovery, score note delivery to named CoreMIDI ports with per-part channels, OSC UDP score note delivery with editable addresses, and an external worker isolated from device callbacks.
- Native HTTPS serving and a documented local CA workflow.
- Interactive `init.sh`: dependency installation attempts, active tool-version checks after upgrades, frontend/release builds, optional tests, startup generation, LaunchAgent/systemd user installation, and `--startup` enable/disable management. `--start` launches the release server in the foreground, reusing a generated startup script when present. `--stop` sends SIGTERM to tracked manual launches from this project, checks process identity to reject stale PIDs, and reports a failure if shutdown exceeds ten seconds per server. Replacing an existing service requires confirmation before regenerating its launch script.
- Plugin trait example, built-in registration documentation, developer instructions, and source-plugin examples inside the DSP crate.

## Incomplete features from the agreed plan

1. **Realtime execution and validation:** rendering currently uses an orchestration worker feeding lock-free device queues, not DSP directly in a high-priority callback. Hardware sample-clock/timestamp mapping, scheduling budgets, graceful overload handling, input drift correction, graph resource estimates, and a 60-minute 32-player load test remain required.
2. **Personal monitoring:** browsers can select the master mix or one of up to 32 dedicated patch monitor outputs. Automatic per-performer sends, mix-minus construction, and monitor assignments remain pending. Microphone loss concealment, adaptive input buffering, per-performer latency calibration, and authoritative monitor-age reporting remain pending. Use headphones; there is no implemented automatic feedback suppression.
3. **Performance modes:** modes and launch permissions exist, but conducted cue distribution, snippets, performer-owned effect controls, transport authority delegation, lookahead, and published performance snapshots need completion. Structured parts begin together; conducted/freeform parts wait for a launch. Commands during playback take effect at the next integer engine beat; commands while stopped or paused apply immediately. Stop resets independent modes to idle.
4. **Live graph editing:** the UI and API support adding/moving/deleting nodes, wiring, structural modal edits, and undo in all active show modes. Prepared replacements install between DSP blocks without waiting for a bar. Structured score/settings changes and all mode changes remain inactive-only; deleting an instrument clears its route. Unchanged parts retain playback position, cues, suspended notes, and synth voice phase/envelopes across replacement. New or musically changed conducted/freeform parts start idle. Unchanged nodes also retain effect/control runtime storage when configuration, input contracts, and latency compensation match. Crossfades, state continuity for edited effect configurations and scores/routes, pending-revision controls, and graph-level delayed feedback are unfinished.
5. **Composition:** the scrolling editor implements chords/voices, display rests, measure splits, ties/tuplets, grace notes, articulations, octave lines, shared signatures, dynamics and MIDI automation. It is not full Finale/Sibelius engraving parity: pagination, nested repeats, arbitrary volta pass lists, percussion/tab staves, independent mid-score staff key changes, and comprehensive collision optimization are absent. MusicXML covers the documented subset; external notation-app interoperability and complex cross-staff engraving still need broader validation. Raw MIDI/graph settings require native JSON. Device/network testing has not established a validated performance score follower.
6. **Control library:** full typed message payloads, ordered fan-out, pack/unpack, routing, bounded event delays, and Pd-compatible trigger/value semantics are not all implemented. Current numeric control nodes evaluate at sample rate.
7. **Remaining audio library:** reusable patch inlets/outlets, standalone biquad/resonant filters, binary spectral arithmetic and additional masking operators, full block feedback, selectable vocoder band count, and latency metadata throughout the SDK are unfinished. Real FFT currently shares the complex FFT implementation, rather than an optimized half-spectrum path.
8. **Collaboration:** revision conflict detection works; fine-grained edit leases, granular operations, shared undo, membership management/revocation, and project export including sample assets need completion.
9. **Integration:** incoming OSC transport and graphical-control messages are implemented with configurable IPv4 bindings; Physical MIDI note/CC graph routing and the two note-specific OSC converters are implemented; generic OSC input/output catalog nodes, arbitrary MIDI/OSC payload mapping, and automatic MIDI reconnection remain pending. Network MIDI session management and timed OSC bundles remain pending. External note delivery is best effort.
10. **SDK/contracts:** built-ins still require an explicit dispatcher branch. Automatic Rust-to-TypeScript generation, declarative plugin registration, full typed parameter metadata, sandboxing, and dynamic loading are not present.
11. **UI telemetry:** no server-side per-modal subscription filtering, min/max audio-rate summaries, robust clock drift regression, per-edge event pulses, or correct undo grouping across an arbitrarily long slider gesture yet.

## Production checkpoint scope (2026-09-07)

Graphical controls now support inline Bang/Integer/Float/Slider/Text input, with modal numeric limits and read-only connected passthrough. Multiple-node duplication, group context menus and Make subgraph are implemented. The libraries use one accordion without the hint footer. Compiled Git identity is printed on startup and exposed through status; manual startup checks the tracked remote and warns in red when stale or unverifiable.

MIDI/OSC System Settings tabs and OSC receiving are **not included** in this checkpoint. Ableton Link has been researched, not implemented. See HANDOFF.md for the accepted next scope and migration instructions.

## Validation record

- Physical-device meters and discovery preferences (2026-09-08): all 88 Rust workspace tests (9 core, 44 DSP, 35 server), nine frontend unit tests, production frontend build, and all 15 Chromium/API integration tests passed. Fixed-array meter tests cover all 64 inputs, transient peaks, sparse output routing, summed peaks/cancellation and empty routes. Browser hardware fixtures cover device grouping, individual physical channel numbering, clipping colors, unavailable capture, telemetry expiry, inactive output removal, and desktop/tablet layout. Settings tests cover default enablement, retained disabled/reconnected choices, immediate checkbox persistence, unsaved draft isolation, rollback, API validation and rediscovery. Desktop/tablet screenshots were inspected. Native devices are disabled in the test server; physical capture, playback and device timing remain manual/unverified.
- Native channel routing, saved revisions and the Monitor redesign: all 86 Rust workspace tests, nine frontend unit tests, the production frontend build, and all 13 Chromium/API integration tests passed. Routing tests cover physical channels 9/64, duplicate inputs, None, 8-to-2 summing with gain headroom, invalid routes, live replacement, callback channel delivery, persistence and undo. Revision tests cover 2,000 edits coalescing into one snapshot, idempotent manual saves, both keyboard shortcuts flushing pending input, stale-write rejection, permissions, and a real one-minute server autosave with the browser page closed. Monitor tests cover transient sample peaks, post-routing clipping, paused output silence, desktop/tablet layout, all three color states, stale telemetry, CPU/resident-memory readouts, and existing WebRTC continuity. The macOS process-stat path was exercised; the Linux-specific memory path was not run. Physical audio-interface routing/listening, true-peak calibration and load/endurance with the new metering/device buffers remain unverified.

- Editor/control/startup checkpoint (2026-09-07): 77 Rust tests (9 core, 43 DSP, 25 server), nine frontend unit tests, ten Chromium/API integration tests, and the frontend production build passed. Tests cover one-sample Bang, numeric/text passthrough, connected edit rejection, numeric limits and integer validation, live widget edits, multi-node D duplication and undo, the three-item group context menu, live grouping with retained control output, typed boundary formats and nested boundary proxies, and library accordion visibility. Bash 3.2 syntax/help and isolated launcher tests passed, including build identity, stale/dirty/offline warnings and unchanged startup settings. Hardware listening and timing remain manual/unverified.


- Input selection, clock control, nested graphs and libraries (2026-09-07): all 76 Rust tests (9 core, 42 DSP, 25 server), six frontend tests, nine Chromium/API integration tests, and frontend/release builds passed. Tests cover separate native input frames and offsets, native route enablement and legacy settings, phase-preserving connected tempo, typed audio/control/spectral boundaries at 1/2/8 channels, and 200 nesting levels with containment-cycle rejection. Browser tests exercise live nested edits, port renaming, independent subtree duplication, deletion/undo, both library drag/drop paths, exact placement, Control-click on macOS, box selection, and moving/deleting multiple nodes. Library API tests verify private access, immutable old versions, irreversible publication, private forks, owner checks, and byte-identical bundled WAV originals copied into a consumer project. Browser device discovery/denial tests use mocks; the existing synthetic WebRTC cancellation regression also passes. Nested-graph and library screenshots were inspected. Physical inputs, real browser permission prompts/microphones, independent device drift and high-load/endurance remain manual/unverified. No startup services or user project data were changed.

- Legacy structured-project live edits (2026-09-07): reproduced and fixed a rejection when a score already referenced a deleted instrument. The structured-mode guard now clears a route only when the current edit deletes an existing node, preserving unrelated legacy references. A failing-before/passing-after Rust regression covers adding/deleting unrelated nodes; the Chromium live-edit test now retains the starter score's missing instrument reference while adding, wiring, deleting, and undoing nodes in all three live modes. Server tests and the release build passed. No user project data or hardware settings were changed.

- Live editing/FM/spectral tools (2026-09-07): all 70 Rust tests (9 core, 38 DSP, 23 server), five frontend tests, five Chromium/API tests, and frontend/release builds passed. Browser coverage adds/wires/removes/undoes nodes during all three modes at 1 BPM (without waiting for the bar), verifies Space activation/play/pause and repeat suppression, text-entry protection, context-menu Edit/Delete, Backspace selection deletion, connected-frequency read-only display, and live signal-type rejection. Spectral browser checks verify multiply-to-zero changes actual frames, drawn magnitude attenuation, keyboard phase edits, persistence, undo, and invalid curve rejection. Rust checks cover sample-rate FM at every channel width, structured score/mode protection including instrument-route deletion/undo, and per-frame spectral math/curve interpolation, representation/generation retention, and conjugate symmetry at 256/1024/8192 bins and 1–8 channels. The curve-editor screenshot was inspected. Browser testing caught and fixed telemetry overwriting uncommitted numeric/curve drafts. Physical playback, live-rewire click suppression, and load/endurance remain unverified.

- Native output buffering (2026-09-07): a deterministic regression reproduces 128 missing frames per 512-frame callback with the old 128-frame DSP setting. The replacement sizes queues from observed callback requests and preserves every sine sample in simulated 44.1/48/88.2/96-kHz streams across all six DSP block sizes and 64–4096-frame callbacks, including two callbacks between worker fills. A DSP regression verifies sine → gain → output remains a scaled sine. All 67 Rust tests, five frontend tests, three Chromium/API tests, and frontend/release builds passed. These are software regressions; physical playback, end-to-end latency, arbitrary scheduling stalls, and independent-device drift remain unverified.

- Update command (2026-09-07): macOS Bash 3.2 syntax/help and conflicting-mode rejection passed, as did the interactive `--startup` leave-unchanged branch. A real noninteractive `--update` completed the TypeScript/Vite and release builds. All 64 Rust tests, five frontend tests, and three Chromium/API integration tests passed. No startup services were changed; hardware validation remains manual.

- Audio setup and visualizers (2026-09-07): 64 Rust tests (9 core, 35 DSP, 20 server), five frontend tests, the TypeScript/Vite production build, and all three Chromium/API integration tests passed. Rust checks cover original-rate conversion duration/DC and anti-alias filtering, packet-boundary-independent resampling, settings validation and bounded project logs, unavailable-route repair, sine harmonic content, identical oscillator output across six render block sizes, numeric/UTF-8 pass-through, unchanged audio at all eight channel widths, block-counted history, and unchanged Cartesian/polar spectral bins, phase, generation, and latency. Browser/API checks cover retained original WAV bytes, 96-kHz/1024-frame preparation, lazy caches in another project, rejection of invalid settings/routes and active-show configuration changes, 44.1-kHz/256-frame WebRTC playback, gear navigation, console popout, waveform choices, audio-edge snapshots, visualizer displays and canvases, numeric/string validation, and read-only connected input. Screenshots were inspected, including `system-audio-settings.png`, `audio-waveform-preview.png`, `visualizer-nodes.png`, and `spectral-visualizer.png`. A settings-load race and SVG/HTML popup namespace issue were caught and corrected during browser testing. Physical output interfaces, loopback latency correction/metronome alignment, and visualizer load/endurance were not tested; earlier 32-client baselines do not cover these additions. No startup services were changed.

- Password minimum: server and registration form now require eight characters. API integration rejects a seven-character password and accepts eight-character bootstrap/invited registration; Chromium signs in with the eight-character credential. All 14 server tests, five frontend tests, both browser/API tests, and frontend/release builds passed after the change.

- Rust unit tests: project/graph validation, clock phase, connected parameter protection, finite audio, disconnect-value carry utility, FFT round trips, streaming reconstruction, zero-gain EQ, silent vocoder modulator, and time-stretched tone frequency/duration.
- Scheduling regression tests: independent same-pitch synth instances, part stop/relaunch, transport reset at beat zero, conducted/freeform idle startup and launch boundaries, local loop positions, duplicate-off suppression after pause, held-note restoration and rapid pause/play, cancellation of suspended notes and cues, graph replacement with preserved lanes and synth voices, shared external pitch ownership with MIDI-channel isolation, and OSC panic note-offs over localhost UDP. These do not validate physical MIDI hardware or network timing.
- DSP state migration: reordered delay/synth/control graph produces sample-identical continuation versus uninterrupted rendering; changed configuration retains new settings. Exhaustive migration checks across every effect and spectral configuration remain pending.
- ADSR: exact attack/decay/release sample counts, early release, retriggering, zero-duration stages, and connected-parameter graph integration. No hardware timing claim is made.
- Step sequencer: rising-edge advance, first-step behavior, wrap, reset priority, disabled-edge handling, length reduction, live value edits, and all three control outputs through the compiled graph.
- Channel mapping: permutation, duplication, mute, unused-channel silence, smoothed live routing changes, and compiled graph equivalence at all widths from 1 to 8. Split–mono processor–merge round trips also pass at every width; incompatible mono/stereo ports are rejected by graph validation. Physical multichannel hardware remains untested.
- Audio admission: Rust exercises capacity including pending offers, 64 simultaneous reservation attempts admitting exactly 32, cleanup after abandoned attempts, and protection against retired cleanup removing a replacement slot. Chromium rejects a duplicate HTTP offer while preserving the existing connection and nonzero received audio energy. This does not establish 32-peer audio throughput or timing.
- Browser audio cancellation: Chromium delays microphone permission using a synthetic AudioContext stream, deactivates the show, connects a replacement monitor, and verifies late tracks stop without disturbing that monitor. It also delays a real server offer response, cancels it, waits for cleanup, and verifies a replacement WebRTC connection. Physical microphone permission/device paths remain manual.
- Dedicated monitors: speaker isolation, mono-to-stereo duplication, stereo selection from wider bundles, and silence for missing outputs. Chromium receives a real selected oscillator cue through Opus/WebRTC.
- Clock ratios: multiplication/division, phase retention during ratio changes and pause, tempo-linked beat boundaries, and reset/restart at zero through the compiled graph. Hardware timing remains unverified.
- Remote show state: Chromium receives activation/deactivation issued through another HTTP client, closes an established WebRTC monitor after deactivation, and recovers an offline deactivation on reconnect.
- Reconnect recovery: Chromium changes a project name and score through a separate HTTP request while the page is offline, then verifies both appear after reconnection without leaving the stage. Queue-overflow recovery uses the same server snapshot path but has not been stress-tested.
- Performance stage: Chromium exercises engine-backed launch/stop, hidden editing controls, monitor connection continuity without a second peer, a 768×1024 layout, and offline timing hold/recovery. This is browser emulation, not physical iPad testing.
- Notation durations: unit coverage for dotted/short values, exact duration sums over 4,096 grid values, and unsupported/nonfinite inputs. Chromium verifies dotted eighth metadata, a rendered tie for 1.25 beats, and the explicit 0.8-beat custom-rhythm label. A delayed-save regression checks that a newer collaborator revision survives delivery of an older HTTP response.
- Part setup: Rust validates names, display values, and loop bounds. Chromium verifies saved settings and preserved note durations, a shortened-loop warning, distinct default displays when switching parts, and read-only stage rendering.
- Project settings: Chromium verifies remote activation disables saving, name/mode/tempo persist through the API, the project picker refreshes, and a concurrent revision blocks the open draft without overwriting remote edits.
- Frontend: TypeScript/Vite production build and numeric input/readout tests.
- Chromium/API integration: initial account, project creation, graph rendering, driven parameter modal, live parameter mutation, transport/telemetry, conducted part launch/stop status, live API graph replacement preserving part playback, WebRTC negotiation and nonzero received audio energy from a selected dedicated cue, notation and piano-roll rendering; invitation redemption, unauthorized writes, stale revisions, WAV upload, phase-vocoder activation, and clef/key/hidden-meter editing with MusicXML export/import verified against the saved server response, including unchanged quarter-note positions through a 6/8 round trip; SVG notehead/playhead alignment for uneven onsets the follow-playback toggle, and persistence of MIDI channel/OSC address edits. Tests use an isolated temporary database and local server.
- Setup: Bash syntax, help output, interactive `--startup` leave-unchanged and generate-only branches, generated launch-script syntax, and an optimized release build. The `--start` addition passed Bash 3.2 syntax/help and leave-unchanged checks, plus isolated mock-server checks for missing builds, conflicting options, noninteractive launch, paths with spaces, environment/saved settings, and exit-code propagation. The `--stop` addition passed macOS Bash 3.2 syntax/help and isolated `python3 tests/test_init.py` checks for direct/saved-script start–stop, repeated stops, stale identity rejection, missing builds, conflicting options, paths with spaces, exit-code propagation, and the interactive leave-unchanged branch. Package-manager installation and LaunchAgent/systemd changes have **not** been executed as tests.
- Launcher host/port and default-port update: Bash 3.2 syntax/help, isolated launcher tests (including invalid arguments, IPv4/IPv6/hostname flag forwarding, component overrides, and saved TLS settings), 14 server tests, release build, five frontend tests, production build, and both Chromium/API integration tests passed. Opt-in `PR0_REAL_SERVER=target/release/pr0-server python3 tests/test_init.py` verified HTTP status on default port 4000 and a custom localhost port overriding an older saved address, then stopped both test servers. TLS hostname resolution and IPv6 socket binding were not exercised by these smoke tests. No startup services were changed.
- 32-client protocol baseline: `npm run test:load` runs an opt-in release server with 32 separate authenticated Chromium contexts, 32 structured synth parts, and 32 dedicated stereo monitors. The 15-second observation passed on this machine: all clients received continuing audio energy, at least 80 packets/second, at least 10 telemetry snapshots/second, and no telemetry gap of 500 ms. The saved report is `docs/load-baseline.json`. This excludes full application rendering, browser microphone uplinks, physical audio devices, LAN transit, and endurance. A synthetic intercepted page initially triggered Chromium local-network restrictions; the harness now loads a real local response. Audio sinks play normally with Chromium output muted.
- 32-client application baseline: `npm run test:load:ui` loads the real Vue workspace in 32 authenticated contexts, opens each assigned part in stage view, and connects its dedicated monitor through the UI. The 15-second run passed with at least 895 playhead position changes per client, no JavaScript errors, maximum telemetry gap 55.7 ms, and maximum animation-frame gap 16.8 ms. Packet loss counters were zero; cumulative concealed-audio samples totaled 9,261 across clients (including connection startup, not isolated to the observation window). See `docs/load-ui-baseline.json`. This is a same-machine headless Chromium run, not a physical LAN/display latency or endurance result.
- 32-client synthetic uplink baseline: `npm run test:load:uplink` passed a 15-second observation with 32 WebCodecs-generated audio tracks sent through real WebRTC connections, server Browser input nodes, and dedicated return monitors while rendering the Vue stage. Every client sent 754–757 packets, received 1,477–1,502 packets and nonzero audio energy, and updated its playhead at least 873 times. There were no JavaScript errors; maximum telemetry gap was 57 ms and maximum animation-frame gap was 33.4 ms. Return-audio loss counters totaled 49 packets; concealed-audio samples increased by 22,022 during observation, so this is not a dropout-free result. See `docs/load-uplink-baseline.json` (2026-09-07 UTC). The five frontend tests and both normal Chromium/API integration tests also passed after this run. Synthetic capture bypasses physical microphone and permission paths; the same-machine software-clock run does not establish LAN latency or endurance.
- Not performed: physical audio-loopback measurements, actual macOS input/output audio checks, physical iPad Safari testing, TouchDesigner session, 32-client physical-device/microphone-uplink load test, or 60-minute soak.

Do not claim the 20 ms score-alignment target, a browser monitoring latency ceiling, or performance readiness on the basis of the current tests.
